# Backend API Configuration
API_URL=http://localhost:8000
API_TIMEOUT=10000

# App Configuration
APP_VERSION=1.0.0
ENVIRONMENT=development

# Firebase (Add your Firebase config)
# These will be in google-services.json (Android) and GoogleService-Info.plist (iOS)
